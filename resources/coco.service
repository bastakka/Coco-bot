[Unit]
Description = Coco

[Service]
Type = simple
Restart = always
RestartSec = 5
WorkingDirectory = /srv/coco
Environment = PYTHONBUFFERED = 1
EnvironmentFile = /etc/venv/coco/bin/activate
ExecStart = /srv/coco/bot.py

[Install]
WantedBy = multi-user.target
